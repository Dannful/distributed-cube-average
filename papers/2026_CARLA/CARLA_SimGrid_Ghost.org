# -*- coding: utf-8 -*-
# -*- mode: org -*-

#+TITLE: SimGrid to Evaluate Ghost Cells Effectiveness in High-Performance Computing Applications
#+AUTHOR: Vinícius Daniel Spadotto

#+STARTUP: overview indent
#+LANGUAGE: en
#+OPTIONS: H:3 creator:nil timestamp:nil skip:nil toc:nil num:t ^:nil ~:~
#+OPTIONS: author:nil title:nil date:nil
#+TAGS: noexport(n) deprecated(d) ignore(i)
#+EXPORT_SELECT_TAGS: export
#+EXPORT_EXCLUDE_TAGS: noexport

#+LATEX_CLASS: LLNCS
#+LATEX_CLASS_OPTIONS: [runningheads]
#+LATEX_HEADER: \usepackage[T1]{fontenc}
#+LATEX_HEADER: \usepackage{graphicx}
#+LATEX_HEADER: \graphicspath{{img/}}
#+LATEX_HEADER: \usepackage{hyperref}
#+LATEX_HEADER: \usepackage{amsmath}
#+LATEX_HEADER: \usepackage{booktabs}

# You need at least Org 9 and Emacs 24 to make this work.
# If you do, just type make (thanks Luka Stanisic for this).

* Reviews                                                          :noexport:
** Deadline for changes Camera-Ready Version Due: July 27th, 2026 GMT
https://carlaconference.org/
** DONE Review 1 (Acceptance)

On the manuscript: Well written, applied best practices for latency
hiding for stencil codes, chose plausible execution platforms, results
consistent to what is expected from the literature. Clean design of
experiment, clean presentation of results. Very good computer science
and HPC methodology. More than deserving of publication.

Not an impediment for current publication, but, I would like to make
some comments that might be useful for future work by the authors,
which are mostly focused on the user/geophysical point of view:

1) The domain sizes are artificially low for the kind of experiments
   the authors seek to investigate, from a geophysicist point of
   view. Most workloads are defined by the smallest unit where the
   system state can be hosted (i.e. domain with all fields), and for
   the biggest size domain, 512^3, this is still a couple GBs, no more
   than 10 GB.

From just a pratical point of view, it would not make sense to split
such small domain among either nodes or devices, since one could
simply take a bit longer on a per-shot basis and get perfect linear
scaling, by distributing one shot per node. This is even more true for
the smaller-sized grids.

This is specially noticeable on the GPU-accelerated cases, since
moving data and communicating through the PCI-e bus is a huge penalty,
which normally is avoided at all costs on production codes. Most
geophysical workflows (not simulations) are throughput-bound, not
latency bound.

It would be very interesting to see authors explore these kind of
analysis for high-resolution elastic simulations, which require much
finer grids and a larger number of fields per grid-point. These
usually do not fit the memory of a single GPU, even datacenter GPUs,
for reasonable frequency ranges, thus inter-accelerator or inter-node
communication becomes an actual necessity, and low network speed
quickly becomes a very real problem, specially for legacy
infrastructure.

#+begin_comment
Future work.
#+end_comment


2) Again, I don't understand why such large number of domain splits,
   at least for geophysical workflows. Assuming 64 RTX 4090 GPUs,
   split among 16 nodes, having one MPI rank per GPU, and exchanging
   halos accordingly, the reasonable grid size to justify that many
   nodes for one single domain would be around 3400^3 (= 64 x 24 GB /
   5 fields / 8 bytes ) for the fletcher acoustic wave equation.

This is way bigger than both: 1) The largest domain size investigated
by the authors; 2) Almost any simulation for wave propagation which is
not fully elastic, given frequency content limitation and the minimum
speed of sound in realistic scenarios. This creates a scenario of
unrealistic network pressure, and could paint a unnecessarily
pessimistic view of the system.

#+begin_comment
Não temos o HW para isso. Future Work.
#+end_comment

3) I don't understand why the authors chose to investigate network
   bandwidths as low as 10 Mbps (Fig. 3), specially in the context of
   HPC systems. Such slow networks can barely be bought for consumer
   settings, the bare minimum being usually around 100/1000 Mbps
   nowadays. It would be useful to understand this choice beyond "it
   is a possible network speed value".

#+begin_comment
É o que temos. Não vamos mudar nada aqui.
#+end_comment

4) It would be interesting, on a future work, for the authors to
   explicitly account for PCI-e bus latency when discussing the
   effects of network on GPU nodes, because there is significant
   penalty in moving data from two gpus inside the same node, just
   from the PCI-e latency. This limitation should provide different
   threshold values than from the network alone.

#+begin_comment
Realmente future work.
#+end_comment

** DONE Review 2 (Revision)

The manuscript addresses runtime optimization in distributed
simulation applications by analyzing the ghost cell asynchronous
communication technique to overlap computation and
communication. Although the work demonstrates the effectiveness of the
algorithm in a specific case study, the problem statement lacks the
specificity necessary to clearly establish the context of the research
and the restrictions under which the proposed solution operates;

It is essential that the environmental conditions and characteristics
of the hardware infrastructure where the algorithm is applicable are
explicitly defined to substantiate the relevance of the study.

Likewise, there is an ambiguity in the methodological approach
derived from the wording of the introduction, which wrongly suggests a
multi-objective combinatorial approach based on variables such as the
size of the problem, the architecture and the bandwidth, for which a
balance is sought that is ultimately not addressed from that
perspective nor does it delve into the details of the specific
algorithm used.

Therefore, it is necessary to define whether the design corresponds to
an experimental research, which would require the formal definition of
a function or goal objective, or to an exploratory study, in which
case the exploration strategy, the observation framework and the
systematic analysis of the results must be made explicit.

In its current state, the narrative adopts the procedural structure of
a technical development report instead of a scientific research
article aimed at solving a generic problem in the area, so reorienting
the document towards standard methodological rigor and a solid
theoretical justification will allow raising the quality of the
manuscript, ensuring its replicability and facilitating its
understanding for the scientific community.

** TODO Review 3 (Acceptance)

This paper discusses the use of SimGrid simulations as a medium to quantify and analyze the effectiveness of computation and communication overlap in halo exchanges present in MPI parallel applications. The authors focus on a particular seismic simulation (Fletcher RTM) to analyze how masking effectiveness is influenced by the number of processing units (CPU or GPUs), problem size, and available interconnect bandwidth.

Strengths

 * The paper presents an interesting approach to using simulations to
   evaluate the impact of network bandwidth changes on communication
   bottlenecks in an application, given a specific hardware
   configuration and problem size.
 * The experimental design is well formulated and accounts for the main
   factors one would consider when analyzing halo exchanges.
 * The heatmap in Figure 6 is a good use of visualization to integrate
   application-dependent parameters (such as problem size and
   parallelization) with hardware characteristics (such as network
   capability and processing performance) for analyzing performance
   behavior.

Weaknesses

 * The description of the modeled clusters is too concise. It is not
   clear if in those clusters, especially the GPU ones (tupi, poti),
   there are multiple GPUs per node and what the network topology is.

 #+begin_comment
TODO -> DONE 3ac3b4b
 
Tentar deixar mais claro isso.
 #+end_comment
 
 * In section 3.2 the authors make the argument that their tests on
   tsubasa agree with what they see in cei. However, the data is not
   really shown to the reader so it is not possible to corroborate
   this. Data should be shown to support their claim.

 #+begin_comment
TODO -> DONE 3602d46
 
Talvez tirar a argumentação sobre a tsubasa.
 #+end_comment
 
 * The paper needs some rephrasing, as some sections require several
   re-reads to grasp the intended meaning. In particular, the section
   where Masking Effectiveness is presented could benefit from
   mathematical notation to establish the metric.

Questions to authors

  * From my understanding, on the modeled GPU machines, you assume all
   neighbor communication occurs over the network. In realistic HPC
   scenarios, one would assume GPU nodes contain multiple accelerators
   interconnected with NVLink. What would the implications be in the
   masking experiments when one considers intranode exchanges?

  #+begin_comment
Communication heterogeneity não foi considerada. Implicações seriam
diversas mas não estudamos isso. Não precisamos fazer nada.
  #+end_comment
  
 * Could the authors provide a little more explanation of Figure 2?
    From a time-trace perspective, it is not entirely clear to me what
   the "height" of each rank bar represents. In particular, on the
   right side plot, does the height of the blue filling represent
   anything? Why do ranks 0-3 and 12-15 seem to have higher blue
   regions than the middle ranks?

 #+begin_comment
TODO ->  2702e69
 
Ótima questão por que não sabemos a resposta para a pergunta "Why do
ranks 0-3 and 12-15 seem to have higher blue regions than the middle
ranks?". Então acho que aqui tu poderia esclarecer o que significa a
altura das barrinhas. Fazer este item a respeito da Figura 2.
 #+end_comment
 
 * The right-hand-side plot shows that ranks 0-3 and 12-15 appear to
   spend more time in MPI_Waitall than ranks 4-11. Could you briefly
   describe why this is?

 #+begin_comment
Good question. Verificar então a hipótese do desbalanceamento de
carga, contando quantas células cada trabalhador tem e ver se esses
números se relacionam com os ranks [4-11] ou com os ranks
[0-3,12-15].

Outra hipótese, é que tenha a ver com a quantidade de vizinhos. Então,
contar quantos vizinhos um determinado rank de [0-15] tem [contar
também o volume de comunicação entre esses vizinhos]. Verificar se
essas contagens se correlacionam com esses grupos visuais (os ranks
[4-11] ou com os ranks [0-3,12-15])
 #+end_comment
 
 * In Section 3.2, you state: "whenever the network bandwidth is
   sufficiently low relative to the available compute throughput, we
   obtain communication masking trivially."
     o When you say compute throughput, do you mean arithmetic
       intensity or compute load? Higher compute throughput (larger
       FLOPS) would mean less time in computation, so less chance to
       hide communication, which is contrary to what you state.

 #+begin_comment
TODO -> 3ed6baf
 
Melhorar o fraseamento. Quanto mais carga de trabalho, mais chance de mascarar.
 #+end_comment
 
 * In general, the paper would benefit from a stronger formulation of
   takeaways for other stencil codes where optimizing
   communication-computation overlap is important.

 #+begin_comment
Não tem como fazer pois envolve "other stencil codes".
 #+end_comment
 
Minor comments

 * This sentence in section 3.1 needs some rework as it is currently
   confusing:
     o "We employ this metric in our results interpretation, as it
       clearly reflects how much communicating hides a particular
       configuration (problem size, number of workers, and network
       capacity) delivers in terms of behavior."
 * In Section 2.3 there is a badly formatted latex expression: $≈$1,GB,

 #+begin_comment
TODO -> DONE 9c66d0b
 
I think we should do this minor comments.
 #+end_comment
   
Overall recommendation

Weak Accept

** TODO Review 4 (Acceptance)

Paper Summary:
This paper presents three main contributions. First, it proposes the design and implementation of a distributed MPI-based application for solving pseudo-acoustic wave equations for seismic imaging in anisotropic media, employing a 3D block decomposition and asynchronous ghost-cell communication. Second, it provides an implementation of this application using the SimGrid/SMPI simulation framework. Finally, it presents an evaluation using SimGrid to characterize the effectiveness of ghost-cell communication masking under several workloads and host configurations, ranging from CPU-only to GPU-equipped clusters. The results show that CPU-only systems can achieve near-perfect communication overlap, whereas GPU-accelerated clusters require network interconnects of at least 10–25 Gbps to avoid becoming network-bound.
Strengths:

 * A wide range of experiments.
 * Interesting performance results.

Weaknesses

 * There is no related work on simulation frameworks or benchmarks that
   use the ghost cells strategy.

 #+begin_comment
Já eraz... vamos deixar como está.
 #+end_comment
 
 * The contributions of this paper are not very significant, and there
   is no clear future direction that justifies this evaluation.

 #+begin_comment
TODO -> 63a5fb0
 
Talvez colocar uma palavra para ajudar a esclarecer a parte "and there
   is no clear future direction that justifies this evaluation".
 #+end_comment
 

Detailed comments

 * Redundant information within the same paragraph: in 'Evaluation
   Methodology and Masking Effectiveness Metric', the variations in
   network configurations, nodes, and workload are repeated
   consecutively.

 #+begin_comment
TODO -> 89668a4
 
Remover a redundância.
 #+end_comment

* Figures                                                          :noexport:
*** Comm. Cost vs. Partitioning Strategy
#+begin_src R :results output :session *R* :exports both :noweb yes :colnames yes :eval no
# Load libraries
library(ggplot2)
library(tidyr)
library(dplyr)

# Define the data
P <- seq(1, 5010, by = 10) # Number of processors
L <- 1 # Normalized side length

# Internal Surface Area Formulas
area_1d <- 2 * (P - 1) * L^2
area_2d <- 4 * (sqrt(P) - 1) * L^2
area_3d <- 6 * (P^(1/3) - 1) * L^2

# Create a data frame and convert to long format for ggplot
df <- data.frame(Processors = P,
                 `Slices` = area_1d,
                 `Columns` = area_2d,
                 `Blocks` = area_3d)
df_long <- pivot_longer(df, cols = -Processors,
                        names_to = "Partitioning_Type",
                        values_to = "Boundary_Area") |>
  mutate(Partitioning_Type =
           factor(Partitioning_Type,
                  levels = c("Slices", "Columns", "Blocks"),
                  labels = c("Slices (1D)", "Columns (2D)", "Blocks (3D)")
                  ))

# Generate the plot
ggplot(df_long, aes(x = Processors, y = Boundary_Area, color = Partitioning_Type)) +
  geom_line(size = 0.8) +
  labs(#title = "Communication Cost vs. Partitioning Strategy",
       #subtitle = "Comparing 1D, 2D, and 3D Decomposition for a Cube",
       x = "Number of Processing Elements (P)",
       y = "Internal Boundary Area\n(Communication Volume\nin communicated items)",
       color = "Partitioning Strategy") +
  theme_bw(base_size=6) +
  coord_cartesian(ylim = c(0, 200)) +
  scale_color_manual(
    values = c("red", "blue", "darkgreen"),
    labels = c("Slices (1D)", "Columns (2D)", "Blocks (3D)")
  ) +
  scale_x_continuous(expand = c(0.01, 0)) +
  scale_y_continuous(expand = c(0, 0)) +
  theme(
    plot.margin = unit(c(0, 0.2, 0, 0), "cm"),
    legend.spacing = unit(0.1, "mm"),
    panel.grid = element_blank(),
    legend.position = "top",
    legend.direction = "horizontal",    
    legend.justification = "left",
    legend.key.size   = unit(0.2, "cm"),
    legend.box.spacing = unit(0, "pt"),
    legend.box.margin = margin(0, 0, 0, 0)
  ) -> p
ggsave("img/comm_cost.png", width = 4, height = 1.3, units = "in", dpi = 300)
#+end_src

#+RESULTS:



#+RESULTS:
* LLNCS configuration for org export + ignore tag (Start Here)     :noexport:

#+begin_src emacs-lisp :results output :session :exports both
(add-to-list 'load-path ".")
(require 'ox-extra)
(ox-extras-activate '(ignore-headlines))
(add-to-list 'org-latex-classes
             '("LLNCS"
               "\\documentclass[runningheads]{llncs}\n [NO-DEFAULT-PACKAGES]\n [EXTRA]\n"
               ("\\section{%s}" . "\\section*{%s}")
               ("\\subsection{%s}" . "\\subsection*{%s}")
               ("\\subsubsection{%s}" . "\\subsubsection*{%s}")
               ("\\paragraph{%s}" . "\\paragraph*{%s}")
               ("\\subparagraph{%s}" . "\\subparagraph*{%s}")))
#+end_src

#+RESULTS:

* Frontpage                                                          :ignore:
#+BEGIN_EXPORT latex
\title{SimGrid to Evaluate Ghost Cells Effectiveness in High-Performance Computing Applications}
%
\titlerunning{SimGrid to Evaluate Ghost Cells in MPI Applications}
% If the paper title is too long for the running head, you can set
% an abbreviated paper title here
%
\author{Vinícius Daniel Spadotto \and \\
Lucas Mello Schnorr\orcidID{0000-0003-4828-9942}}
%
\authorrunning{V. D. Spadotto et al.}
% First names are abbreviated in the running head.
% If there are more than two authors, 'et al.' is used.
%
\institute{Institute of Informatics, Federal University of Rio Grande do Sul, Porto Alegre, Brazil \\
\email{\{vdspadotto,schnorr\}@inf.ufrgs.br}}
%
\maketitle
#+END_EXPORT

* The Paper                                                          :ignore:
** Abstract                                                         :ignore:

#+LaTeX: \begin{abstract}

Ghost cell communication, the asynchronous exchange of halo regions
between neighboring MPI processes, is the canonical technique for
overlapping computation with communication in distributed stencil
applications. Whether this overlap masks communication effectively
depends on the relationship between problem size, partitioning
topology, and interconnection bandwidth, a balance that is difficult
to characterize without access to diverse large-scale hardware. This
paper uses a distributed MPI implementation of the Fletcher RTM
seismic application with 3D block decomposition and asynchronous ghost
cell communication, together with a calibrated SimGrid/SMPI simulation
framework that enables the exploration of network configurations
beyond those physically available.
#+latex: %
Experiments across four host configurations reveal two contrasting
regimes: CPU-only machines naturally achieve near-perfect
communication hiding due to modest floating-point throughput, while
GPU-equipped clusters invert the bottleneck entirely, with accelerator
throughput outpacing the network and stalling compute load near 20% on
1 Gbps interconnects regardless of problem size: a deficit only
recovered at 100 Gbps. Partitioning topology experiments further show
that prime process counts are as detrimental as slow networks, with
slice decompositions falling below single-node throughput baselines
irrespective of bandwidth. Together, these results establish
interconnection bandwidth and partitioning topology as co-determining
factors for ghost cell masking effectiveness, and confirm SimGrid/SMPI
as a practical capacity-planning tool when diverse real infrastructure
is unavailable.

#+latex: \keywords{SimGrid \and Ghost Cells \and HPC Applications \and MPI \and Stencil Computation}

#+LaTeX: \end{abstract}

** Introduction

Halo exchange is a foundational technique for distributing iterative
stencil computations across HPC nodes. Each MPI process maintains a
\emph{ghost-cell} layer, a local copy of its neighbors' boundary data,
and asynchronously overlaps these transfers with interior
computation. Whether this overlap achieves genuine communication
masking depends on the interplay of problem size, partitioning
topology, and interconnection bandwidth: with small domains or slow
networks, communication dominates; with large domains and fast
networks, computation can fully hide the transfers
\cite{stencil_supercomputers}. Quantifying this balance requires
systematic exploration of those parameters across hardware
configurations, precisely the kind of exploration that is difficult to
perform on shared, resource-constrained large-scale HPC clusters. The
challenge has motivated dedicated benchmarks such as MiniGhost
\cite{minighost}, a stencil miniapp from the Mantevo suite \cite{heroux2009improving} designed to
explore boundary exchange strategies across problem sizes and process
counts. However, even miniapps require real hardware at scale to
reach observations.

Real HPC infrastructure is expensive, access is time-limited, and it
generally uses fixed hardware configurations. Exploring whether a
100Gbps InfiniBand interconnect would change the masking behavior of
a given application is infeasible when only a 1Gbps Gigabit cluster
is available. The same constraint applies to topology studies:
evaluating a 64-node partition when only five nodes are accessible
requires either analytical modeling, which frequently cannot capture
real memory hierarchy and OS noise, or simulation.

SimGrid/SMPI \cite{simgrid} \cite{simgrid_smpi} addresses this gap by
intercepting MPI communication calls and simulating them on a
configurable virtual platform. At the same time, the application's
computation code executes natively on the host hardware. This strategy
is a calibrated emulation, also known as online simulation, and
therefore not the typical abstract modeling: computation timings are
observed in situ and injected into the simulation, while network
parameters originate from a systematic platform calibration procedure
\cite{simgrid_calibration}. SimGrid's goal is to enable the study of
existing MPI applications within the comfort of the simulator
\cite{simgrid_lower_barriers}. SimGrid's performance prediction
capabilities already exist on communication-intensive benchmarks, most
prominently HPL \cite{hpl_emulation} \cite{hpl_prediction}.

Despite this breadth, published SimGrid use cases tend to focus on
collective operations \cite{degomme2017simulating},
scheduling \cite{dutot2015batsim}, and load
balancing \cite{keller2017using}. Ghost cell and halo exchange studies
exist, but they consistently rely on real hardware or alternative
frameworks. Using SST/macro, one line of work simulates halo exchange
patterns in atmospheric models and notes that SST/macro's packet-level
model and SimGrid produce near-identical communication timing results,
yet the study targets scalability of collective patterns rather than
computation-communication overlap in stencil
kernels \cite{gmd_halo}. SimGrid's SimDag framework has been applied
to evaluate process placement for MPI applications with halo-exchange
communication patterns, focusing on topology-aware optimization of
process affinity rather than masking
effectiveness \cite{simdag_affinity}. SimGrid SMPI has also been used
to predict the performance of stencil computations on heterogeneous
platforms, again centering on placement and mapping strategies rather
than the computation-communication overlap
question \cite{topology_aware}. On real hardware, blocking versus
non-blocking MPI halo exchange has been benchmarked on national
supercomputers, confirming that overlap is achievable but inherently
hardware-specific \cite{blocking_nonblocking_halo}. To the best of our
knowledge, SimGrid/SMPI has not been used to evaluate the
effectiveness of ghost cell masking in MPI stencil applications. Since
SMPI emulates computations between MPI calls on real hardware and
simulates the communication layer with configurable parameters, it is
well-suited for this purpose, and this is the gap the present paper
addresses.

We selected the Fletcher RTM application as the vehicle to make the
evaluation concrete and non-trivial. Fletcher solves pseudo-acoustic
wave equations for seismic imaging in anisotropic media
\cite{fletcher}, applying an eighth-order
finite-difference stencil over a discretized 3D grid at each time
step. The application is computationally intensive and memory-bound
per iteration, and benefits strongly from GPU acceleration
\cite{multi-gpu} \cite{okamoto2010}. When paired with a slow interconnect,
it creates precisely the bandwidth asymmetry that makes ghost cell
masking most challenging to achieve. Using a real, numerically
validated application rather than a synthetic benchmark ensures that
the communication patterns and memory access behaviors observed are
representative of production workloads.

The contributions of this paper are threefold: *(i)* we briefly present
the design and implementation of a distributed MPI implementation of
Fletcher with 3D block decomposition and asynchronous ghost cell
communication, numerically validated against single-node reference
results; *(ii)* a calibrated SimGrid/SMPI simulation framework for that
application, validated against real cluster executions within a 15%
error margin; and *(iii)* an evaluation that employs SimGrid to
characterize ghost cell masking effectiveness across four host
configurations spanning CPU-only and GPU-equipped clusters, revealing
that CPU-only machines achieve near-perfect communication hiding as a
natural consequence of arithmetic intensity, while GPU-accelerated
clusters demand at least 10--25 Gbps interconnects to escape a
network-bound regime, and that partitioning topology, specifically the
avoidance of prime process counts, is as decisive as bandwidth for
achieving scalable overlap. Collectively, these contributions open a
practical simulation-based methodology for capacity planning in
distributed stencil applications that use halo exchange, bridging
the gap between costly real-cluster experimentation and abstract
analytical models.

*** Paper structure                                                :ignore:

Section [[#sec.methods]] outlines the methods and materials used, which
include the theoretical background on partitioning, ghost cells, and
the Fletcher equations, along with the MPI application design and the
SimGrid simulation methodology. Section [[#sec.results]] includes our
findings, detailing the experimental methodology in the simulations as
well as the results and metrics in the form of the case studies to
evaluate the effectiveness of the ghost cells implementation for
masking communication. Finally, Section [[#sec.conclusion]] addresses the
limitations of our approach and concludes the document by offering
some perspectives on our work.

# The companion material of this work is publicly available at \COMPANION.

** Methods and Materials
:PROPERTIES:
:CUSTOM_ID: sec.methods
:END:

This section describes the methods and materials used to build and
evaluate the distributed Fletcher RTM application with ghost cells
using the SimGrid simulation framework. We first establish the
theoretical foundations, static domain partitioning, ghost cell
communication, and the Fletcher pseudo-acoustic stencil, before
detailing the MPI application design that realizes these concepts and
the SimGrid simulation framework used to evaluate performance beyond
the constraints of available hardware.

*** Theoretical Background
**** Partitioning

Static partitioning divides the problem domain once at the start of
execution and keeps the subdivision fixed throughout
\cite{grama2003}. Since the Fletcher stencil kernel applies the same
operation uniformly across the grid, each worker's workload is
homogeneous within its sub-volume, and dynamic rebalancing offers no
benefit. Among the possible 3D decomposition strategies -- Slices
($\{P, 1, 1\}$), Columns ($\{\lfloor P^{1/2}\rfloor, \lfloor P^{1/2}\rfloor, 1\}$), and Blocks ($\{\lfloor
P^{1/3}\rfloor, \ldots\}$) -- the 3D Block decomposition minimizes the total
surface-to-volume ratio of the sub-domains. The total communication
boundary for slices grows as $2(P-1)L^2$, while for blocks it grows as
$6(\sqrt[3]{P}-1)L^2$ \cite{stencil_supercomputers}. Figure
\ref{fig:comm_cost} depicts the communication cost (Y-axis) as a
function of the number of processing elements (PEs, in the X-axis) for
the three strategies (color), where we can see how the 3D approach
scales much better and its advantages appear for small number of
PEs. Because the stencil volume grows as $O(n^3)$ while the halo
surface grows as $O(n^2)$, the communication-to-computation ratio
decreases as problem size grows, provided the decomposition remains
cubic. The ~MPI_Dims_create~ and ~MPI_Cart_create~ functions create a 3D
partitioning that automatically distributes the process count across
the three axes and establishes a Cartesian communicator. A known
pitfall arises with prime process counts: since one cannot use primes
to factor into a balanced cube. We need to pay attention to avoid
using prime numbers for rank counts, as ~MPI_Dims_create~ degenerates to
a $\{P,1,1\}$ slices decomposition, negating the surface-area
advantage of block partitioning.

#+begin_export latex
\begin{figure}[!htb]
\includegraphics[width=\linewidth]{./img/comm_cost.png}
\caption{Comparing the communication cost (Y axis) generated by the
    three partitioning strategies (color) as a function of the number
  of processors (X axis).}
\label{fig:comm_cost}
\end{figure}
#+end_export

**** Ghost Cells

Ghost cells extend each process's local grid with a thin shell of
boundary data received from neighboring processes
\cite{ghost_cells}. Although the local stencil does not update these
extra cells, the memory overhead justifies the latency savings. By
prefetching boundary data before it is immediately required, processes
can initiate communication at the start of each iteration and overlap
it with interior computation. The overlap strategy proceeds as follows
within each time step: (1) post non-blocking receives for incoming
halos; (2) compute boundary strips, the cells whose values will be
sent to neighbors, using the previous iteration's complete data; (3)
send boundary strips asynchronously; (4) compute the interior while
halos are in transit; (5) wait for incoming halos, insert them, and
swap wavefield vectors. The critical requirement for effective masking
is that the interior computation time exceeds the halo transfer time;
when the network becomes the bottleneck, step (5) stalls, and
communication dominates the iteration.

**** Fletcher Modeling

Reverse Time Migration (RTM) reconstructs subsurface reflectivity by
propagating seismic wavefields forward and backward in time and
cross-correlating them \cite{rtm}. In isotropic media the wave
equation reduces to $\frac{\partial^2 p}{\partial t^2} = v^2 \nabla^2 p$; anisotropic
media introduce spatially-varying parameters $\epsilon(x,y,z)$ and $\delta(x,y,z)$
that vary the wave speed with propagation direction
\cite{anisotropy}. Fletcher's pseudo-acoustic formulation
\cite{fletcher} couples the pressure field $p$ with an auxiliary field
$q$ through:

#+BEGIN_EXPORT latex
\begin{gather}
  \frac{\partial^{2}p}{\partial t^{2}} = v_{px}^{2}H_{2}p + \alpha v_{px}^{2}H_{1}q + v_{sx}^{2}H_{1}(p - \alpha q) \label{eq:fletcher_p} \\
  \frac{\partial^{2}q}{\partial t^{2}} = \frac{v_{pn}^{2}}{\alpha}H_{2}p + v_{px}^{2}H_{1}q - v_{sx}^{2}H_{2}\!\left(\frac{1}{\alpha}p - q\right) \label{eq:fletcher_q}
\end{gather}
#+END_EXPORT

where $v_{pz}$, $v_{pn}$, $v_{px}$ are derived P-wave velocities, $v_{sx}$ is the
shear velocity, and $H_1$, $H_2$ are differential operators encoding,
respectively, the anisotropic and isotropic components of the stencil
\cite{fletcher}. The parameter $\alpha$ is set to 1 following
Fletcher's recommendation. The stencil applies this update at every
grid point using an eighth-order finite-difference scheme, advancing
both $p$ and $q$ by one time step $\Delta t$.

*** HPC Application Design with Ghost Cells

The distributed application follows a coordinator-worker model over
MPI. The coordinator, typically in rank 0, partitions the global $N_x
\times N_y \times N_z$ domain using ~MPI_Dims_create~ and dispatches compact
metadata packets, local dimensions, global offsets, absorption zone
parameters, and seismic source position, to all workers via
point-to-point sends. Each worker independently allocates and
initializes its sub-volume, including the four wavefield arrays
($p_{\text{past}}$, $p_{\text{curr}}$, $q_{\text{past}}$, $q_{\text{curr}}$) and
the precomputed anisotropy coefficients; no wavefield data is
distributed, eliminating a potential memory bottleneck on rank 0
during the initialization phase as this application uses synthetic
data only.

The main computation loop implements the five-step ghost cell overlap
strategy described above. Each worker posts non-blocking MPI receives
(~MPI_Irecv~) at the start of each iteration for all 26 neighbors in the
3D Cartesian grid; boundary strips are computed and dispatched via
~MPI_Isend~; interior computation proceeds concurrently; and a final
~MPI_Waitall~ synchronizes all transfers. Because the coordinator
participates in the same Cartesian topology as the workers, it
executes the same computation loop with no special treatment during
the time-stepping phase.  If the ghost-cell strategy for masking
communication is effective, the accumulated time in the ~MPI_Waitall~
operation for any rank should be nearly zero for all ranks.

A CUDA backend replaces the OpenMP propagation kernel when GPU nodes
are available. Wavefield vectors reside permanently in device memory;
only the halo buffers travel between device and host via 3D strided
copies during send/receive operations. The system maps boundary and
interior sub-regions to separate parameterized CUDA kernel launches,
preserving the same overlap logic as the CPU backend. Numerical
validation was performed by comparing SHA-256 checksums (OpenMP
backend, 30 configurations of 1–6 workers across five problem sizes
from $32^3$ to $512^3$) and per-element delta checks within $10^{-15}$
tolerance (CUDA backend), confirming bitwise and near-bitwise
reproducibility against the single-node reference implementation.

*** Simulation with SimGrid

The simulation framework builds on SimGrid/SMPI \cite{simgrid}. We
implement a virtual platform for SimGrid with parameters that adapt it
to different in-premise cluster configurations. The main parameters
are the host speed (in GFlops) and the network bandwidth (in
Gbps). Since we run all simulations on a single node per cluster and
the compute code runs either with OpenMP or CUDA, the speed parameter
for all hosts in the virtual platform matches that of the host running
the simulation. Rather than replacing compute kernels with analytical
FLOP counts, which proved an oversimplification that ignored cache
behavior, PCIe transfers, and memory copies, we leverage SimGrid's
native computation sampling: the main propagation kernel executes in
full for a variable number of warm-up iterations, after which the code
injects stable timing samples into the simulation. We vary the network
bandwidth parameter to reflect our intentions when evaluating masking
effectiveness. Since the virtual platform accepts customized values
after a calibration procedure, we follow a methodology
\cite{simgrid_calibration} to extract MPI runtime parameters, protocol
thresholds, bandwidth, and latency from a systematic suite of
microbenchmarks. We adopt these calibrated values throughout the
simulations. Our post-calibration validation shows that the platform
description reproduces the real cluster's behavior within a 15% error
margin for message sizes up to $\approx$1~GB, which we consider acceptable
since application behavior, both compute and communication, remains
very homogeneous throughout execution, indicating that trends stay
sufficiently faithful to evaluate communication masking effectiveness.

** Results
:PROPERTIES:
:CUSTOM_ID: sec.results
:END:

We first describe the evaluation methodology and introduce the Masking
Effectiveness metric used throughout the analysis. We then present the
results for the CPU-only host, =cei=, followed by the
two GPU-equipped clusters, =poti= and =tupi=. Finally, we present a joint
heatmap visualization of Masking Effectiveness across all GPU
experimental conditions, enabling direct identification of optimal
configurations (problem size, host count, bandwidth).

*** Evaluation Methodology and Masking Effectiveness Metric

We define two designs of Experiments (DoE) depending on whether the
host machines have GPUs or are solely CPU-based. If the host machine
used for simulation has a GPU, the GPU DoE uses a full factorial
design, crossing three independent factors to yield 120 unique
experimental conditions. The first factor, *(1) problem size*, has four
levels (64, 128, 256, and 512), where each level corresponds to the
order of the cubic problem domain. The second factor, *(2) number of
hosts*, spans five levels (4, 8, 16, 27, and 64), ranging from a small
cluster (4 nodes) to a larger deployment (64 nodes). The choice of
these levels aligns with the 3D partitioning. The third factor, *(3)
network bandwidth*, takes six levels (1, 2.5, 10, 25, 50, and 100
Gbps), covering an exceptionally wide range spanning four orders of
magnitude, from slow interconnects typical of commodity or congested
networks up to high-speed links found in modern HPC environments.
If the host machine has only CPU cores, the CPU DoE
differs due to lower compute power. It also follows a full factorial
structure, crossing the same three factors to produce 128 unique
experimental conditions. In
both DoE projects, the application executes 1000 iterations, the
network latency remains fixed at 22\micro seconds (taken from our real
network measurements), and each combination appears exactly once, with
no replication, and is assigned a sequential run order. The design
aims to jointly characterize how performance scales with workload
size, platform scale, and network capacity in a distributed computing
or HPC context, with particular emphasis on understanding the impact
of network bandwidth across a broad and heterogeneous range of speeds,
including high-compute-capacity (large number of GPUs) and CPU-only
clusters.

We consider these hosts to run the simulations with their respective
compute configurations: =tupi= (1\times GPU RTX4090 per node, 6 nodes total), =poti= (1\times GPU RTX4070 per node, 5 nodes total), and =cei= (CPU 2\times Xeon Silver 4116 per node, 6 nodes total), with the nodes being interconnected in a star-shaped topology via a switch.
When running the simulations on a GPU-equipped machine, we run the
optimized CUDA code. When in CPU-equipped mode, we run the OpenMP
kernel that exploits all CPU cores (both logical and physical). We run
all simulations using calibrated MPI communication values based on the
real =poti= cluster network and per-DoE network bandwidth
configurations.

The *Masking Effectiveness* metric derives from MPI communication
traces. Figure \ref{fig:masking-example} depicts two examples of
simulation runs to depict the metric rationale. SimGrid generates
these traces using built-in, easy-to-use features. We first isolate
the core execution window, defined as the interval between the
earliest =MPI_Irecv= start and the last =MPI_Waitall= end across all
ranks, and then clip all events to that window. We infer a Compute
state for each container by identifying gaps between consecutive MPI
communication events. For each rank, we sum the total communication
duration, and the overall makespan (from the first to the last event)
across all ranks, and the Communication ratio is obtained by dividing
the aggregate communication duration by the aggregate
makespan. Finally, we define Masking Effectiveness as 1 minus that
Communication ratio, representing the fraction of execution time
during which communication overlaps with computation: the closer to 1,
the more effectively the application masks its communication
overhead. We employ this metric in our results interpretation, as it
clearly indicates whether the MPI operations are the current bottleneck or not.
Figure \ref{fig:masking-example} shows
exactly this perception: on the left, a very communication-bound run
with masking of only 0.0819; on the right, a very good example where
masking is 0.792.

#+begin_export latex
\begin{figure}[!htb]
\includegraphics[width=\linewidth]{./img/masking-example.png}
\caption{The Masking Effectiveness rationale from a space/time view of the MPI Application behavior, where the load of each operation of every rank (Y-axis), represented by the height of the bars, denotes the amount of MPI communication (blue) or computation (yellow) being done at a specific point of time (X-axis). On the left, a communication-bound \texttt{tupi} case with 16 nodes with a 1 Gbps network bandwidth depicting a very poor Masking Effectiveness of 0.0819. On the right, a compute-bound case with a 100 Gbps network bandwidth, with a much better Masking Effectiveness of 0.792.}
\label{fig:masking-example}
\end{figure}
#+end_export

From a practical perspective, we employ modern data science tools
based on the R ecosystem, primarily leveraging the tidyverse and
ggplot2 stack to process all MPI traces obtained from
SimGrid/SMPI. Raw trace files generated during execution are first
converted into structured tabular representations, enabling scalable
manipulation of the events and the computation of our metric.

*** CPU Cluster: Communication Masking Progression

Figure \ref{fig:cei-masking} illustrates the results for the =cei=
machine (a CPU-only host equipped with dual Xeon Silver 4116
processors), revealing a striking pattern: whenever the per-step
compute time is sufficiently large relative to the communication
time - e.g., because compute throughput is low or the workload's
arithmetic intensity is high - communication is trivially masked,
since it can be fully overlapped with the (longer) computation.
Across all problem sizes (64³ to 512³) and host counts (8 to 64), Masking
Effectiveness remains below 0.30 for bandwidths at or below 100 Mbps,
reflecting configurations in which communication dominates and there
is little computation to hide it behind. However, as soon as bandwidth
exceeds 1 Gbps threshold, the picture changes dramatically: for the
largest problem size (512³) with only 8 hosts, Masking Effectiveness
leaps to approximately 0.94, and even moderately sized problems (256³,
8 hosts) reach values above 0.89 at 1 Gbps. This sharp transition
reveals that the CPU-only cei cluster sits in a compute-bound regime
for most realistic HPC network configurations. Once the network is
fast enough to deliver data quickly, the relatively modest per-core
floating-point throughput of the Xeon Silver 4116 ensures that
computation fills the gaps between communication events almost
entirely, yielding near-perfect masking. Put differently, on a slow
CPU-only cluster, communication masking is essentially free: the
application's arithmetic intensity naturally outpaces the network,
enabling overlap to emerge naturally, making this class of hardware
surprisingly forgiving from an MPI communication-hiding perspective.

#+begin_export latex
\begin{figure}[!htb]
\includegraphics[width=\linewidth]{./img/cei-masking.png}
\caption{The Masking Effectiveness (in the Y-axis) as a
   function of the network bandwidth capacity (X-axis),
   the number of MPI ranks (colors), and problem
   size (facets). Case for the \texttt{cei}
   host configuration.}
\label{fig:cei-masking}
\end{figure}
#+end_export

*** GPU Cluster: The Network Bottleneck

Figure \ref{fig:poti-masking} illustrates the results for the =poti= GPU
cluster, revealing the opposite regime. With RTX4070 accelerators
paired with a 1Gbps Gigabit network (first X-axis coordinate), the
masking effectiveness stalls at a very low regime, at around 0.25,
regardless of problem size: $128^3$, $256^3$, and $512^3$ all yield
the same result. Even the largest problem (512³) with only 4 hosts
barely reaches 0.16, and the metric deteriorates further with more
hosts, confirming that the RTX4070 already outpaces the Gigabit link
regardless of domain size: the RTX4070 finishes the local stencil
computation so rapidly that the Gigabit network cannot deliver ghost
cells in time. =MPI_Waitall= stalls therefore dominate each of the 1K
iterations. This behavior precisely matches the saturation described
in the theoretical background: the volume-to-boundary ratio offers no
relief because the GPU's throughput overwhelms any benefit from larger
domains. Our simulations indicate that recovery begins at 10 Gbps,
where 512³ with 4 hosts reaches 0.97 and stays there through 100
Gbps. However, smaller problems and larger host counts demand even
higher bandwidth to recover meaningful overlap due to their less
favorable communication-to-computation ratio. The poti data thus
reveal that to escape the network-bottleneck regime on our =poti= GPU
cluster requires at least a 10 Gbps or 25 Gbps interconnect, and even
then only under favorable problem-size and node-count conditions.

#+begin_export latex
\begin{figure}[!htb]
\includegraphics[width=\linewidth]{./img/poti-masking.png}
\caption{The Masking Effectiveness (in the Y-axis) as a
   function of the network bandwidth capacity (X-axis),
   the number of MPI ranks (colors), and problem
   size (facets). Case for the \texttt{poti}
   host configuration.}
\label{fig:poti-masking}
\end{figure}
#+end_export

Figure \ref{fig:tupi-masking} illustrates the results for the =tupi=
cluster, powered by RTX4090 GPUs. The results broadly mirror the
network-bottleneck regime observed on the =poti= cluster: at 1 Gbps,
Masking Effectiveness collapses across all configurations, with even
the largest problems barely exceeding 0.16 for 4 hosts and falling
below 0.08 for 27 or more, reflecting an accelerator so fast that the
Gigabit link is perpetually the gating factor. Meaningful overlap
resurfaces only at 10 Gbps and above, where small host counts and
large domains recover values above 0.96. These are consistent with the
poti pattern, but shifted toward demanding even higher bandwidth due
to the RTX 4090's greater raw throughput. However, =tupi= exposes a
striking anomaly absent from =poti=: Masking Effectiveness keeps
improving from 256³ to 512³ only with larger host counts (27 and 64)
for network speed 25 Gbps and larger. Lower host counts (4, 8, and 16)
have a poorer metric in 512³. This reversal of the expected scaling
behavior is likely governed by the same volume-to-surface ratio
dynamics that drive communication efficiency in domain
decomposition. As the problem size grows from 256³ to 512³, the
interior compute volume scales as the cube of the domain edge, while
the ghost-cell communication surface grows only as its square. With
few hosts, each rank owns a large subdomain and already benefits from
a favorable ratio, so doubling the edge length adds proportionally
more communication surface than it does useful compute to overlap,
degrading masking. With many hosts, each rank's subdomain is small and
surface-dominated at 256³, and the jump to 512³ finally brings enough
interior work to amortize the communication cost, pushing Masking
Effectiveness upward.

#+begin_export latex
\begin{figure}[!htb]
\includegraphics[width=\linewidth]{./img/tupi-masking.png}
\caption{The Masking Effectiveness (in the Y-axis) as a
   function of the network bandwidth capacity (X-axis),
   the number of MPI ranks (colors), and problem
   size (facets). Case for the \texttt{tupi}
   host configuration.}
\label{fig:tupi-masking}
\end{figure}
#+end_export

*** Masking Effectiveness Heatmap: Find Optimal Configurations

Figure \ref{fig:gpu-masking-heatmap} presents a joint visualization of
the Masking Effectiveness across all experimental conditions for the
two GPU-equipped hosts, =poti= and =tupi=, as a function of network
bandwidth (X-axis), number of hosts (Y-axis), and problem size
(facets). The heatmap format allows one to immediately pinpoint, for
any given configuration explored in our DoE, whether communication is
effectively hidden by computation or whether the application stalls
waiting for ghost cells. The viridis color scale, from dark purple
(poor masking) to bright yellow (near-perfect masking), reveals a
consistent transition pattern: the lower-left region of each panel,
corresponding to low bandwidth and many hosts, is uniformly dark,
confirming network-bound stalls regardless of problem
size. Conversely, the lower-right region, large domains, few hosts,
and high bandwidth concentrates the bright cells, where masking
effectiveness exceeds 0.90. The precise identification of this
transition boundary is one of the key contributions of the
SimGrid/SMPI simulation approach: rather than requiring exhaustive
real-cluster experiments across dozens of network configurations, the
calibrated simulator allows us to navigate the full factorial space
and pinpoint exactly which (size, hosts, bandwidth) triplets deliver
effective communication hiding, information that would be
prohibitively expensive to obtain on real infrastructure alone.

#+begin_export latex
\begin{figure}[!htb]
\includegraphics[width=\linewidth]{./img/masking_heatmap_gpu.pdf}
\caption{Masking Effectiveness for \texttt{poti} (RTX4070) and \texttt{tupi} (RTX4090) across
all (problem size, hosts, bandwidth) configurations. Color encodes Masking Effectiveness from 0
(communication-bound, dark) to 1 (computation-bound, bright).
The sharp transition from dark in the lower-left to bright in the
lower-right delineates the network bottleneck boundary.}
\label{fig:gpu-masking-heatmap}
\end{figure}
#+end_export

** Conclusion
:PROPERTIES:
:CUSTOM_ID: sec.conclusion
:END:

This paper presented a distributed MPI implementation of the Fletcher
RTM application with 3D block decomposition and asynchronous ghost
cell communication, alongside a calibrated SimGrid/SMPI simulation
framework for exploring network configurations beyond those physically
available. Experiments across four host configurations (cei, poti, and tupi) spanning both CPU-only and GPU-equipped clusters
exposed two sharply contrasting regimes. On CPU-only machines, modest
floating-point throughput naturally yields near-perfect communication
hiding across most network conditions: the cei cluster reaches
approximately 87% compute load at 512³ on its real 10 Gbps network, establishing that arithmetic intensity alone is sufficient to mask communication on this class of
hardware without any special tuning. On GPU-equipped clusters, the
picture inverts entirely: the raw throughput of RTX4070 and RTX4090
accelerators makes network bandwidth the governing bottleneck, with
both poti and tupi stalling near 20% compute load on a 1 Gbps
interconnect regardless of problem size.

Our design of experiments, sweeping network bandwidth across six
levels from 1 Gbps to 100 Gbps, demonstrated a clear and systematic
progression toward computation-bound behavior, with Masking
Effectiveness values exceeding 90% achieved at 100 Gbps on the GPU
clusters. The joint heatmap of Masking Effectiveness across all
(problem size, host count, bandwidth) triplets provides a single,
immediately readable map of the transition boundary between
network-bound and compute-bound regimes, a kind of capacity-planning
chart for practitioners deciding whether a network upgrade is
worthwhile for a given workload. This is precisely where
SimGrid/SMPI's value is most visible: by emulating computations
between MPI calls on real hardware while simulating the communication
layer with freely configurable parameters, it navigates the full
factorial experimental space at a fraction of the cost of real-cluster
deployments. To the best of our knowledge, SimGrid/SMPI has not
previously been used to evaluate the effectiveness of ghost cell
masking in MPI stencil applications; the present work demonstrates
that it is not only applicable but highly informative for this
purpose, and that calibrated simulation is a practical and credible
alternative to exhaustive real-hardware experimentation.

# The partitioning topology experiment further revealed that prime
# process counts are as detrimental as slow networks: a 5-worker {5,1,1}
# slices decomposition fell below the single-node throughput baseline
# regardless of simulated bandwidth, establishing process count
# selection as a co-determining factor alongside interconnection
# bandwidth for ghost cell masking effectiveness.

Future work includes experiments on the Santos Dumont supercomputer to
evaluate the application on a production platform with a high-speed
interconnect, detailed SimGrid modeling of the Santos Dumont network
topology, and integration of HIP kernels to evaluate AMD GPU
architectures within the same simulation framework. We also intend to
extend this framework to a broader class of stencil-based applications,
establishing a reusable capacity-planning tool that allows practitioners
to simulate ghost cell communication patterns and guide hardware
procurement decisions without requiring access to production-scale
infrastructure.

** Acknowledgments                                                  :ignore:

#+LATEX: \section*{Acknowledgements}

All experiments in this work used the PCAD platform,
http://pcad.inf.ufrgs.br, at INF/UFRGS. This work was partially
supported by Petrobras through an agreement with UFRGS under 2.20.2205
(361), SAP 23078.450001/2022-17, and SIC 2020/00182-5.
This study was financed in part by the ``Coordenação de
Aperfeiçoamento de Pessoal de Nível Superior'' Brasil (CAPES) --
Finance Code 001.

* References                                                        :ignore:

#+LATEX: \bibliographystyle{splncs04}
#+LATEX: \bibliography{biblio}

* Emacs setup                                                      :noexport:

# Local Variables:
# eval: (add-to-list 'load-path ".")
# eval: (require 'ox-extra)
# eval: (ox-extras-activate '(ignore-headlines))
# eval: (add-to-list 'org-latex-classes '("LLNCS" "\\documentclass[runningheads]{llncs}\n [NO-DEFAULT-PACKAGES]\n [EXTRA]\n" ("\\section{%s}" . "\\section*{%s}") ("\\subsection{%s}" . "\\subsection*{%s}") ("\\subsubsection{%s}" . "\\subsubsection*{%s}") ("\\paragraph{%s}" . "\\paragraph*{%s}") ("\\subparagraph{%s}" . "\\subparagraph*{%s}")))
# End:
